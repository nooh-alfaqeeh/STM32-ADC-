## 1st task
Use potentiometer and implement:
Polling mode
Interrupt mode
DMA mode

## 2nd task 
Use ADC + DMA to control a servo motor using a potentiometer.
the ADC value must be mapped to the servo angle range and used to update the PWM.

## 3rd task
Use Timer PWM and DMA to automatically update the PWM duty cycle from a predefined lookup table.
The LED must fade in and out without CPU modifying the compare register manually.

